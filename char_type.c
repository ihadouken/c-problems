#include <stdio.h>
/* Computes number of characters of certain types from given input */

int main() {
    int i, j, c;
    int 

    while((c = getchar()) != EOF) {
        if (c == '\n' || c == ' ' || c == '\t') 

        else if (c >= '0' && c <= '9')

        else if()
    }

    return 0;
}

